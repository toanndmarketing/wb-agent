version: '3.8'

services:
  app:
    build: .
    image: google-seo-tool:latest
    container_name: google-seo-tool-app
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env
    environment:
      - NODE_ENV=development
